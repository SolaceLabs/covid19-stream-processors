spring:
  cloud:
    stream:
      function:
        definition: retrieveAndPublish
      bindings:
        retrieveAndPublish-out-0:
          destination: com/covidtracking/states/current/get/raw
          content-type: application/json
solace:
  java:
    host: 'tcp://mr2r9za6fwi0wf.messaging.solace.cloud:55555'
    msgVpn: covid-19-streams
    clientUsername: solace-cloud-client
    clientPassword: uklgjh8hv2i6u2d0tp0lavesq4
logging:
  level:
    root: info
    org:
      springframework: info

